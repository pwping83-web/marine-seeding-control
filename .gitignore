# Dependencies
node_modules/

# Build
dist/
dist-ssr/
*.local

# Env (secrets — commit only .env.example if you add one)
.env
.env.*
!.env.example

# 나라장터 공고 알림 스크립트 상태(로컬 전용)
scripts/.g2b-bid-watch-state.json
scripts/.gov-announce-watch-state.json
scripts/.g2b-watch-schedule-marker.json
scripts/.g2b-watch-schedule-marker

# 성장 어드바이저 로컬 주기 상태
scripts/.growth-advisor-state.json

# 관공서 제출용 캡처(PNG 용량·민감정보 — 필요 시 zip으로만 제출)
관공서-제출용/captures/*.png

# 지식재산·조달 로컬 메모(민감정보 — 예시만 커밋)
docs/납품-IP-전략/개인정보-로컬메모.md

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Vercel (local CLI state)
.vercel
